---
글쓴사람: 김범진(PM)
날짜: 2026-05-20 23:00
type: notice
---
<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">내용</span><hr style="flex:1;margin:0"></div>

> [!important] 김범진(PM) — 아키텍처 문서 대규모 업데이트
> 오늘 파이프라인 설계 확정 및 기존 문서 불일치 전부 수정했습니다. 각자 담당 모듈 확인해주세요.

> [!note] 김범진(PM)
> ## 1. 신규 파일 추가 — 각자 구현 필요
>
> | 파일 | 담당 (정) | 내용 |
> |---|---|---|
> | `src/engine/postprocess.py` | 박창준(Exp) | NMS / confidence 필터링 / max_detections. `predict.py`에서 분리 |
> | `src/utils/config.py` | 김범진(PM) | `load_config()` + null 검증. 내가 구현 예정 |
> | `src/utils/collate.py` | 김범진(PM) | DataLoader collate_fn. 내가 구현 예정 |
> | `src/utils/validate.py` | 김범진(PM) | 배치 형식 검증. 내가 구현 예정 |
> | `tests/mock_dataset.py` | 공통 | MockDataset 텐서. 현재 `tests/check_yolo.py`에 있는 거 pytest로 이전 예정 |
>
> ## 2. 제거된 파일
>
> - `src/utils/visualize.py` 삭제 → 시각화는 `notebooks/02_visualize_bbox.ipynb` 인라인으로 처리 (ADR-007)
>
> ## 3. 파이프라인 분리 — Step 2가 2a / 2b로 나뉨 (ADR-008)
>
> - **Step 2a**: `predict.py` → `postprocess.py` → `evaluate.py` + target_dict → mAP → `experiment_log.md`
> - **Step 2b**: `predict.py` → `postprocess.py` → `make_submission.py` → `submission_vX.csv` + `predictions.json`
>
> 자세한 흐름은 `architecture/pipeline.md` 참고.
>
> ## 4. evaluate.py 담당 분리 (ADR-009)
>
> - **Step 1 (학습 중 val mAP)**: 유재열(Model) 정
> - **Step 2a (예측 후 성능 계산)**: 박창준(Exp) 정
>
> 같은 파일 두 곳에서 쓰는 거 맞습니다. 중복 구현 아님.
>
> ## 5. src/engine/predict.py 역할 변경
>
> - 기존: "이미지 → pred_dict 리스트" (후처리 포함)
> - **변경**: "1 batch 예측 → raw predictions" (후처리는 postprocess.py로 분리)
> - `train.py`의 1 epoch 단위와 동일한 방식 — 박창준(Exp) 확인 바람
>
> ## 6. 소통 채널 — 카톡 → 디스코드
>
> 앞으로 팀 내 모든 소통은 **Discord**로 합니다. `interfaces.md` 변경 필요하면 디스코드로 먼저 알려주세요.

<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">대상</span><hr style="flex:1;margin:0"></div>


- [x] 전체
- [ ] 김범진(PM)
- [ ] 박창준(Exp)
- [ ] 유재열(Model)
- [ ] 황원재(Data)
- [ ] 없음

<div style="display:flex;align-items:center;gap:12px;margin:16px 0"><hr style="flex:1;margin:0"><span style="font-weight:bold;white-space:nowrap">댓글</span><hr style="flex:1;margin:0"></div>

