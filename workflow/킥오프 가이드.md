---
type: concept
title: "킥오프 가이드 — 역할별 지금 당장 할 일"
created: 2026-05-20
updated: 2026-05-26
tags:
  - workflow
  - onboarding
status: mature
related:
  - "[[workflow/태스크 관리]]"
  - "[[workflow/팀원 역할 분담]]"
  - "[[architecture/인터페이스 계약서]]"
---

# 킥오프 가이드 — 역할별 지금 당장 할 일

> GitHub Issues: https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues
> 이슈 시작 시 Project 보드 → **In Progress** / 막히면 이슈 코멘트 + **Blocked** (이유 필수)

---

# v0.2 — 성능 개선 킥오프

## 황원재 (Data)

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감"
FROM "tasks"
WHERE contains(assignee, "황원재") AND milestone = "v0.2"
SORT priority ASC
```

**순서**
1. **[#52](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/52)** — YOLO 포맷 라벨 변환 스크립트 ← **여기서 시작**
   - COCO JSON → YOLO txt 변환, `data/yolo/` 폴더 구조 생성, train/val 80:20 분리
   - Ultralytics 전환(#51) 담당자(김범진)와 폴더 구조 맞춰서 진행
2. **[#53](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/53)** — 데이터 augmentation 추가
   - #52 완료 후 진행 / Albumentations 파이프라인 강화

---

## 유재열 (Model)

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감"
FROM "tasks"
WHERE contains(assignee, "유재열") AND milestone = "v0.2"
SORT priority ASC
```

**순서**
1. **[#54](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/54)** — 더 큰 모델 실험 (yolov8s/m) ← **여기서 시작**
   - Ultralytics 전환(#51) 완료 후 진행
   - `configs/default.yaml`의 `model.name` 변경으로 실험, 결과는 실험 로그(#56)에 기록
2. **[#55](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/55)** — 하이퍼파라미터 튜닝
   - #54 완료 후 최적 모델 기준으로 lr, epochs 조정

---

## 박창준 (후처리)

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감"
FROM "tasks"
WHERE contains(assignee, "박창준") AND milestone = "v0.2"
SORT priority ASC
```

**순서**
1. **[#57](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/57)** — postprocess.py Ultralytics 호환 리팩터링 ← **여기서 시작**
   - Ultralytics 전환(#51) 완료 후 진행
   - Results 객체 → pred_dict 변환 구조로 재설계
2. **[#58](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/58)** — TTA 적용
   - `model.predict(augment=True)` 적용, #57 완료 후 진행
3. **[#59](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/59)** — WBF 앙상블
   - 유재열님 모델 실험(#54) 완료 후 yolov8n + yolov8s/m 예측 합산
4. **[#56](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/56)** — 실험 로그 작성
   - 모델별 실험 결과를 `reports/experiment_log.md`에 지속 기록

---

## 김범진 (PM)

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감"
FROM "tasks"
WHERE contains(assignee, "김범진") AND milestone = "v0.2"
SORT priority ASC
```

**순서**
1. **[#51](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/51)** — Ultralytics 네이티브 API 전환 ← **여기서 시작**
   - `train.py` → `YOLO().train()`, `predict.py` → `YOLO().predict()` 전면 재작성
   - `configs/pill.yaml` 생성 (Ultralytics data yaml)
   - 황원재(#52)와 폴더 구조 협의 필수
2. **[#60](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/60)** — v2 제출
   - v0.2 개선 전부 완료 후 Kaggle 재제출

---

# v0.1 — 파이프라인 완성 킥오프 (완료)

## 황원재 (Data/Dataset) — zipdid

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감", milestone as "마일스톤"
FROM "tasks"
WHERE contains(assignee, "zipdid")
SORT priority ASC
```

**순서**
1. **[#8](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/8)** — 원본 데이터 구조 확인 ← **여기서 시작**
   - `data/raw/` 파일 구조·annotation 컬럼·bbox 형식·클래스 수 파악
   - 이슈 코멘트에 결과 남기기 → **이게 끝나야 #11 시작 가능**
2. **[#9](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/9)** — EDA 노트북 (`notebooks/01_eda.ipynb`)
3. **[#10](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/10)** — bbox 시각화 (`notebooks/02_visualize_bbox.ipynb`)
4. **#8 완료 후** → #11 → #12 → #13 (Dataset / transforms / split)
   - 참고: [[architecture/인터페이스 계약서]] — Dataset 출력 형식 고정

---

## 유재열 (Model/Train) — YuJY9897

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감", milestone as "마일스톤"
FROM "tasks"
WHERE contains(assignee, "YuJY9897")
SORT priority ASC
```

**순서**
1. **[#22](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/22)** — YOLOv8 설치 및 예제 실행 (30분, 데이터 없이 가능)
   - `pip install ultralytics` → 예제 실행 → GPU 여부 이슈 코멘트 남기고 close
2. **[#23](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/23)** — `build_model()` 스켈레톤 (데이터 없이 가능)
   - `src/models/baseline.py` 생성, 더미 텐서로 forward 통과 확인
   - 참고: [[architecture/인터페이스 계약서]] 섹션 3
3. **#8 완료 후** → #14 → #15 → #16 → #17 (model → train → evaluate → checkpoint)
   - 참고: [[architecture/인터페이스 계약서]], [[architecture/모듈 설계]]

---

## 박창준 (Inference) — cjkj1234

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감", milestone as "마일스톤"
FROM "tasks"
WHERE contains(assignee, "cjkj1234")
SORT priority ASC
```

**순서**
1. **[#24](https://github.com/beomjinkim2000/Code_IT_Team_1_FirstProject/issues/24)** — `predict.py` 스켈레톤 (데이터 없이 가능)
   - `src/engine/predict.py` 생성, 더미 반환값으로 형식만 맞추기
   - bbox는 반드시 **xyxy 형식** — `src/utils/bbox.py` 변환 함수 사용
   - 참고: [[architecture/인터페이스 계약서]] 섹션 4
2. **#11 완료 후** → #18 (실제 모델과 연결해서 predict 완성)

---

## 김범진 (PM) — beomjinkim2000

```dataview
TABLE issue as "#", title as "작업", st as "상태", priority as "우선순위", target as "마감", milestone as "마일스톤"
FROM "tasks"
WHERE contains(assignee, "beomjinkim2000")
SORT priority ASC
```

---

## 공통 규칙

| 상황 | 행동 |
|------|------|
| 이슈 시작할 때 | Project 보드 → **In Progress** |
| PR 올릴 때 | 본문에 `closes #번호` 명시 |
| PR 리뷰 요청 | 팀장 + 본인 외 팀원 1명 지정 (자기 PR 자기 리뷰 불가) |
| 막혔을 때 | 이슈 코멘트에 이유 + Project 보드 → **Blocked** |
| interfaces.md 변경 필요 시 | 팀장에게 디스코드 먼저, PR에서 변경 |

---

## Connections

- [[workflow/태스크 관리]]
- [[workflow/팀원 역할 분담]]
- [[architecture/인터페이스 계약서]]
