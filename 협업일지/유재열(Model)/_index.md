---
title: "협업일지 — 유재열(Model)"
tags: [협업일지]
---

# 협업일지 — 유재열(Model)

```dataview
TABLE file.link as "파일", file.mtime as "날짜"
FROM "협업일지/유재열(Model)"
WHERE file.name != "_index"
SORT file.name DESC
```
