<%*
const authorMap = {
  "beomjinkim2000": "김범진(PM)",
  "cjkj1234": "박창준(Exp)",
  "YuY9897": "유재열(Model)",
  "zipdid": "황원재(Data)"
};
let gitUser = "";
try {
  gitUser = (await tp.system.exec_command("git config user.name")).trim();
} catch(e) {
  gitUser = await tp.system.prompt("GitHub ID를 입력하세요");
}
const author = authorMap[gitUser] || gitUser;
await tp.file.rename(tp.date.now("YYYY-MM-DD") + "-" + author);
-%>
---
type: post
date: <% tp.date.now("YYYY-MM-DD") %>
author: <% author %>
---

# <% tp.date.now("YYYY-MM-DD") %>

---

✍️ 

---

 - <% author %>
