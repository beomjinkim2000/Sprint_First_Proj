현재 프로젝트 torch 가 cuda가 지원되지 않음

1. 학습할 PC에만 cuda 가능한 torch를 설치한다.
- 학습을 돌릴 PC에 cuda 가능한 torch를 따로 설치
- no sync를 통해 uv lock에 torch를 무시하고 cuda 가능한 torch로 구동


1. 환경 자체를 cuda가 가능한 torch를 설치하도록 함 (권장)
- 수정 후 pull 필요
- no sync 불필요


Ultralytics는 갓갓이었다...
Ultralytics래퍼를 쓰면 난 코드3줄로 모델을 호출한다...

Ultralytics를 안쓰고 loss를 구현하기엔 너무 어렵단다.
Ultralytics에서 loss를 구하는 내부 클래스만 호출하는게 좋단다...
그걸 하려면 형식을 변환해야 한단다
3가지인데 생각보다 어렵진 않다.
오늘로 난 느꼇다.
Ultralytics는 갓갓이다.
전처리 후처리든 뭐든 다해준다.

아니... 밥도 안먹고 개빡으로 했는데... 그때도 5시간 한도 다써서 2시에 다시했는데....
17:37 현재 왜....
3시간 반만에 또 다썻냐고...
왜 19시 이후에 해야 하냐고...
강제휴식이잖아.......